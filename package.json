{
  "name": "dr-khatri-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/generate-static-routes.mjs && node scripts/validate-seo.mjs",
    "preview": "vite preview",
    "seo:validate": "node scripts/validate-seo.mjs"
  },
  "dependencies": {
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest"
  },
  "engines": {
    "node": ">=20 <25"
  }
}
