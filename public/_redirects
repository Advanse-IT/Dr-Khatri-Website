# Cloudflare Pages SPA Configuration
# This rule ensures that all routes are handled by the React Router on the client side
# The 200 status code means "rewrite silently" - the URL stays the same but serves index.html

/* /index.html 200
